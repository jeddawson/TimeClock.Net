@model IEnumerable<TimeClock.Models.Holiday>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Repeats)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Repeats)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HolidayID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HolidayID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HolidayID })
        </td>
    </tr>
}

</table>

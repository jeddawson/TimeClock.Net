@model IEnumerable<TimeClock.Models.PayType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DailyMax)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WeeklyMax)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DailyMax)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WeeklyMax)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PayTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PayTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PayTypeID })
        </td>
    </tr>
}

</table>
